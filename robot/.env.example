# このファイルを .env にコピーして値を入れてください（.env は共有しないこと）
#
# 値の場所: Supabase ダッシュボード → Settings → API
#
# ★ anon キーだけを使います。service_role キーは絶対にラズパイに置かないこと ★
#   anon で足ります（RLS が select / insert / update を全員に許可しているため）。

SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOi...
